Those files define common variables for the roles which do not depend on the 
environment: production, live, test, dev. That means, servers in the inventory
with those names will share these global variables.
