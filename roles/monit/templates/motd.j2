#!/bin/sh
# {{ ansible_managed }}

echo " * System managed with monit!"
{% if monit_monitoring_list %}
echo "   Use 'monit stop/stat/status' to manage:"
echo
while read line; do
    echo "   * ${line}"
done < "{{ monit_ansible_monitors_file }}"
{% endif %}
echo
